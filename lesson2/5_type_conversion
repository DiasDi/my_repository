"""Приведение типов

type() - специальная функция в Python, с помощью которого можно узнать ТИП переменной к которому она относится.

str() - специальная функция в Python, с помощью которого можно привести переменную в тип строку (если она валидна)
int() - специальная функция в Python, с помощью которого можно привести переменную в тип число (если она валидна)
bool() - специальная функция в Python, с помощью которого можно привести переменную в тип булев (если она валидна)
"""


# Приведение числа в строку

age = 10
age_str = str(age)

print(type(age))  # class 'int'
print(type(age_str))  # class 'str'


# Приведение строки в число

year = "2023"
year_int = int(year)

print(type(year))  # class 'str'
print(type(year_int))  # class 'int'

# Приведение числа в булев тип

t = 1  # Все что больше или меньше 0 это True
f = 0

t_bool = bool(t)
f_bool = bool(f)

print(t_bool)  # True
print(f_bool)  # False

print(type(t_bool))
print(type(f_bool))

# Пример невалидного приведения типа:

today_is = "Пятница"
# Код ниже вызовет ошибку. Уберите комментарий ниже для проверки:
# today_is_int = int(today_is)
